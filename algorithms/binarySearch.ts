/**
 * A divide-and-conquer search algorithm. Finds a specific value out of n input values by dividing
 * the inputs in 2 each time.
 *
 * O(log n) running time
 */

const binarySearch = (inputArray: Array<number>, searchValue: number) => {

}